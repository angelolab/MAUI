% take mask inputs from GUI and update mask paramaters (i.e. ez_segmentParams)
% in PointManager instance (pipeline_data.points)
function set_mask_parameters_points(blur_value, threshold_value, minimum_value, pipeline_data)

end